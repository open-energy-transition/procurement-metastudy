# -*- coding: utf-8 -*-
# SPDX-FileCopyrightText: Contributors to PyPSA-Eur <https://github.com/pypsa/pypsa-eur>
#
# SPDX-License-Identifier: MIT

# This file is used to define the scenarios that are run by snakemake. Each entry on the first level is a scenario. Each scenario can contain configuration overrides with respect to the config/config.yaml settings.
#
# Example
#
# custom-scenario: # name of the scenario
#   electricity:
#       renewable_carriers: [wind, solar] # override the list of renewable carriers

# ======== Baseline model without procurement ========
# ====================================================

baseline-3H:
  enable:
    procurement: false

baseline-3H-nores-target:
  enable:
    procurement: false

  procurement:
    res_target: false

baseline-1H:
  enable:
    procurement: false

  clustering:
    temporal:
      resolution_sector: 1H

baseline-3H-storage:
  enable:
    procurement: false

  electricity:
    StorageUnit: [li-ion battery, iron-air battery, vanadium, lair, pair] # 
    Store: [H2]

# ======== Baseline model with volume matching ========
# =====================================================

vol-match-PL-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Poland:
        location: "PL0 0"

vol-match-DE-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Germany:
        location: "DE0 0"

vol-match-FR-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      France:
        location: "FR0 0"

vol-match-ES-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Spain:
        location: "ES0 0"

vol-match-DK-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Denmark:
        location: "DK0 0"

vol-match-IE-3H:
  enable:
    procurement: true

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Ireland:
        location: "IE3 0"

vol-match-PL-DE-FR-ES-DK-IE-3H:
  enable:
    procurement: true
  
  solving:
    mem_mb: 150000
    runtime: 24h

  procurement:
    strategy: vol-match
    scope: "all"
    energy_matching: 100

    load:
      participation: 70
      profile: "baseload"

    ci:
      Poland:
        location: "PL0 0"
      Germany:
        location: "DE0 0"
      France:
        location: "FR0 0"
      Spain:
        location: "ES0 0"
      Denmark:
        location: "DK0 0"
      Ireland:
        location: "IE3 0"

# ======== Baseline model with 247 CFE matching ========
# ======================================================

247-cfe-3H-IT:
  procurement:
    strategy: 247-cfe
    scope: "all"
    energy_matching: 100

    load:
      participation: 10

    ci:
      Italy:
        location: "IT0 0"

    strip_network: true

247-cfe-3H-DE:
  procurement:
    strategy: 247-cfe
    scope: "all"
    energy_matching: 100

    load:
      participation: 10

    ci:
      Germany:
        location: "DE0 0"

    strip_network: true

247-cfe-3H-DE-FR-IT:
  procurement:
    strategy: 247-cfe
    scope: "all"
    energy_matching: 100

    load:
      participation: 10

    ci:
      Germany:
        location: "DE0 0"
      France:
        location: "FR0 0"
      Italy:
        location: "IT0 0"

    strip_network: true

# ======= Baseline model with emission matching =======
# =====================================================

emi-match-3H-IT:
  procurement:
    strategy: emi-match
    scope: "all"
    energy_matching: 100
    emission_matching: 100

    load:
      participation: 10

    ci:
      Italy:
        location: "IT0 0"

emi-match-3H-DE:
  procurement:
    strategy: emi-match
    scope: "all"
    energy_matching: 100
    emission_matching: 100

    load:
      participation: 10

    ci:
      Germany:
        location: "DE0 0"

emi-match-3H-DE-FR-IT:
  procurement:
    strategy: emi-match
    scope: "all"
    energy_matching: 100
    emission_matching: 100

    load:
      participation: 10

    ci:
      Germany:
        location: "DE0 0"
      France:
        location: "FR0 0"
      Italy:
        location: "IT0 0"
